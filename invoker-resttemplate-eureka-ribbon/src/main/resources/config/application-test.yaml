server:
  port: 10000
  servlet:
    context-path: /
spring:
  application:
    name: invoker-eureka-resttemplate-ribbon
#http编码配置
http:
  encoding:
    charset: UTF-8
    force: true
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/