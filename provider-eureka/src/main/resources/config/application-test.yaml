server:
  port: 9001
  servlet:
    context-path: /
spring:
  application:
    name: provider-eureka
#http编码配置
http:
  encoding:
    charset: UTF-8
    force: true
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/